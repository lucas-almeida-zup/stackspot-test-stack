type: app
description: Default stackfile usecase description
# template used to create the project
template: stackspot-test-stack/<template-name>
inputs:
  some_input: value
# list of plugins to be applied after project creation
plugins:
  - plugin: stackspot-test-stack/<plugin-name>
    inputs:
      some_input: value
      some_list:
        - item1
        - item2
